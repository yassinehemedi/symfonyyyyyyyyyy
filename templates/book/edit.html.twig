{% extends 'base.html.twig' %}

{% block body %}
    <h1>update a New Book</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">update Book</button>
    {{ form_end(form) }}
{% endblock %}
